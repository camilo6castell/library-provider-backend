# MongoDB connection is configured via the MONGODB_URI environment variable.
# See app-main/src/main/resources/application.yaml for the default value.
# Example (local): MONGODB_URI=mongodb://localhost:27017/library-provider
# Example (Atlas):  MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/library-provider
